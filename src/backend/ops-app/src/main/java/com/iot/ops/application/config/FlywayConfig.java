package com.iot.ops.application.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class FlywayConfig {
    // Flyway is auto-configured via spring.flyway properties in application.yml
    // This configuration class is a placeholder for custom migration callbacks
}
